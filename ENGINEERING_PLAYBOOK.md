# دليل البناء الهندسي المعتمد — Engineering Playbook

> طريقة عملنا القياسية لبناء مواقع ومنصّات الويب. مبنية على مشروع **عبور** كتطبيق مرجعي.
> الهدف: نفس الجودة، وقت أقل، أخطاء لا تتكرّر. هذا الملف هو المرجع الملزِم للفريق.
>
> **كيف تستخدمه في مشروع جديد بنفس الستاك:** انسخ **البنية** لا الكود — عبور هو «القالب المرجعي». اقرأ الأقسام 7→10 (المعمارية + i18n + CMS + السكشن فيو) قبل أول سطر، واتبع قائمة «تشغيل مشروع جديد» (القسم 15).

---

## 1. الستاك المعتمد (Canonical Stack)

| الطبقة | الاختيار | نسخة عبور | ملاحظة |
|--------|----------|-----------|--------|
| الواجهة | **Next.js** (App Router, React) + TypeScript | Next 16.2 / React 19.2 / TS 5 | SSR/ISR، توجيه ملفّي، `output: "standalone"` |
| التنسيق | **Tailwind CSS** | v4 | خصائص منطقية لدعم RTL |
| الباك إند | **Django + DRF** | Django 5.1 / DRF 3.15 | admin جاهز (django-unfold)، ORM ناضج |
| قاعدة البيانات | **PostgreSQL** | 17 | ذاتية الاستضافة، psycopg 3 |
| التخزين | **MinIO** (S3-compatible) | — | django-storages + boto3؛ الكود لا يتغيّر عن S3 |
| الحاويات | **Docker + Compose** | — | خرج standalone للواجهة |
| الحافة | **nginx + Cloudflare** | — | reverse proxy + SSL/CDN + DNS |
| التعدّد اللغوي | نظام i18n مخصّص | — | cookie locale + `pick()` (لا مكتبة خارجية) |
| التحليلات | GA4 + Search Console | — | gtag مؤجّل + لوحة داخل الـCMS عبر service account |

> ثبّت نسخ الأدوات (Node، Python، Postgres) مطابِقة للإنتاج. اختلاف النسخة = مفاجأة عند النشر.
> **قاعدة Next الحديث:** APIs قد تختلف عن تدريب النماذج/ذاكرتك — اقرأ `node_modules/next/dist/docs/` قبل الكتابة (راجع `AGENTS.md`).

---

## 2. خط سير البناء — 7 مراحل بالترتيب

الترتيب مقصود. **لا تنتقل لمرحلة قبل استيفاء معايير قبول السابقة.**

### المرحلة 0 — ترجمة الديزاين (Figma → Code)
- [ ] استخراج **رموز التصميم** (ألوان، مسافات، خطوط، نصف أقطار) كمصدر واحد → `tailwind` config.
- [ ] مكوّن لكل قسم في التصميم، بأسماء مطابقة لأسماء الفريمات.
- [ ] لا ألوان/مقاسات «سحرية» في الكود — كلها من الرموز.

### المرحلة 1 — الواجهة (Frontend)
- [ ] App Router + TypeScript.
- [ ] **RTL/LTR وثنائية اللغة من البداية**: كوكي اللغة، `pick(ar, en)`، اصطلاح بيانات `_EN`.
- [ ] استخدم الخصائص المنطقية (`ms-`, `me-`, `ps-`, `start`, `end`) لا (`ml`, `left`…).
- [ ] كل نص مرئي له نسختان (عربي/إنجليزي) — لا نص مكتوب مباشرة (hard-coded) بلغة واحدة.
- [ ] توحيد التسمية عبر الهيدر/الفوتر/مسار التنقّل/`H1`/الـ metadata.

### المرحلة 2 — الباك إند ونموذج المحتوى (Backend)
- [ ] Django + DRF، فصل: **الطلبات** (الفورمات) عن **المحتوى** (CMS).
- [ ] جسر `fetchContent` مع **سقوط آمن** إلى بيانات ثابتة عند تعطّل/بطء الباك إند.
- [ ] كل حقل محتوى ثنائي اللغة (`*_ar` / `*_en`).
- [ ] مهلة (timeout) على نداءات الباك إند من الواجهة — لا تعليق الرندر.

### المرحلة 3 — لوحة التحكّم (CMS)
- [ ] محرّر مبني على **schema** (عرّف الحقول → المحرّر يتولّد).
- [ ] مصادقة **بالتوكن** (لا كوكيز) — تعمل خلف أي بروكسي.
- [ ] **مبدأ المرآة**: اللوحة تعكس كل صفحة بالكامل، بترتيب الصفحة، بلا زحام ولا حقول مخفية.
- [ ] **سكشن فيو**: كل قسم = عمودان [حقول | معاينة حيّة مطابقة للموقع] (القسم 10).
- [ ] «استعادة الافتراضي» عبر لقطات (snapshots)، ونسبة اكتمال لكل قسم.
- [ ] نفس هوية الموقع (ألوان/شعار/خط) داخل اللوحة.
- [ ] responsive على الموبايل — تُختبر فعليًا.

### المرحلة 4 — دليل المستخدم (Admin Guide)
- [ ] دليل بالصور خطوة بخطوة، ثنائي اللغة، قابل للبحث والطباعة PDF.
- [ ] يغطّي كل شاشة وكل نوع عنصر (عنوان، نص، أيقونة، صورة، كرت، رقم…).
- [ ] **محمي خلف مصادقة اللوحة** — لا يراه إلا الأدمن.

### المرحلة 5 — النشر الذاتي (Deployment)
- [ ] `Dockerfile` لكل خدمة + `docker-compose.yml` + `.dockerignore`.
- [ ] `.env.example` كامل لكل خدمة (قيم وهمية)، والأسرار الحقيقية خارج git.
- [ ] بورتات الحاويات مربوطة على `127.0.0.1` فقط.
- [ ] nginx بروكسي عكسي؛ العامة يصل 80/443 فقط.
- [ ] Cloudflare للـ DNS/SSL؛ الواجهة تُبنى بمسار API نسبي `/api`.
- [ ] نسخ احتياطي مجدوَل (قاعدة البيانات + التخزين).

### المرحلة 6 — التكامل مع الـ ERP (القادم)
- [ ] حدود API واضحة لكل موديول (عقد مكتوب أولًا).
- [ ] طبقة **محوّل (adapter)** لكل موديول تعزل تفاصيل الـERP.
- [ ] مزامنة عبر أحداث/مهام مع سقوط آمن.
- [ ] ابدأ بموديول واحد → أثبت النمط → عمّم.

---

## 3. المبادئ الحاكمة (10)

1. **ثنائية اللغة و RTL** من اليوم الأول.
2. **اللوحة تعكس الموقع 1:1** وبنفس الهوية.
3. **رموز التصميم** مصدر واحد للحقيقة.
4. **الحاويات و`.env.example`** من الأسبوع الأول.
5. **لا سرّ في git** أبدًا — أمثلة فقط.
6. **كل واجهة أدمن خلف مصادقة** — بلا استثناء.
7. **التوثيق والدليل جزء من التسليم**.
8. **مسارات API نسبية** للقابلية للنقل.
9. **سقوط آمن** عند تعطّل الباك إند.
10. **اختبر قبل أن تقفل أو تسلّم** (Playwright على الموقع المباشر).

---

## 7. معمارية النظام (Architecture)

```
المتصفح
  │  (RTL/LTR حسب كوكي اللغة)
  ▼
Next.js (App Router, standalone) ── server components تجلب المحتوى وقت الرندر
  │        │
  │        └── fetchContent()  ──►  Django/DRF (المحتوى + الطلبات)  ──►  PostgreSQL
  │                 (سقوط آمن ↩ بيانات ثابتة لو الباك بطيء/واقع)          MinIO (صور)
  ▼
/cms (React client)  ──(Token auth)──►  نفس Django (endpoints الـCMS)
```

**طبقات الواجهة:**
- `src/app/**` — صفحات App Router (server components افتراضيًا؛ `"use client"` عند الحاجة فقط).
- `src/components/**` — مكوّنات العرض (ومكوّنات الـCMS تحت `src/components/cms/`).
- `src/lib/*Data.ts` — **البيانات الثابتة (fallback)** لكل نوع محتوى + الأنواع (types) + دوال الخرائط (mapping) من شكل الـAPI لشكل العرض.
- `src/lib/server/*` — كود الخادم (`fetchContent`, `loadBranch`…).
- `src/lib/cms/*` — عميل الـCMS (api, i18n اللوحة, icons, schema types).
- `src/i18n/*` — نظام اللغة.

**طبقات الباك إند (`backend/content/`):**
- `m_*.py` — الموديلات (كل حقل ثنائي `*_ar`/`*_en`).
- `api_*.py` — DRF serializers + list views للموقع العام (قراءة).
- `a_*.py` — تسجيل الأدمن (django-unfold).
- `cms_*.py` — واجهة الـCMS: `cms_crud.py` (CRUD عام + توليد schema)، `cms_api.py`، `cms_account.py`، `cms_analytics.py`.

**قاعدة ذهبية:** كل نوع محتوى له **مصدر ثابت** في `*Data.ts` بنفس شكل الـAPI؛ الصفحة تحاول الـAPI أولًا وترجع للثابت لو فشل — فالموقع **لا يكسر أبدًا** حتى لو الباك واقع.

---

## 8. نظام ثنائية اللغة (i18n)

نظام مخصّص خفيف — **بدون مكتبة**. المفاتيح:

```ts
// src/i18n/config.ts
export type Locale = "ar" | "en";
export function pick<T>(locale: Locale, ar: T, en: T): T { return locale === "ar" ? ar : en; }

// src/i18n/locale.ts  (server) — اللغة من كوكي
export async function getLocale(): Promise<Locale> {
  const store = await cookies();
  return store.get("locale")?.value === "en" ? "en" : "ar";  // العربي افتراضي
}
```

**القواعد:**
- **لا نص hard-coded بلغة واحدة.** كل نص إمّا `pick(locale, "عربي", "English")` أو من قاموس `src/i18n/dict/`.
- النصوص المتكرّرة (هيدر/فوتر/أزرار) في **قاموس** (`common.ts`) بشكل `{ ar: {...}, en: {...} }`، والنوع مشتق: `export type Common = (typeof common)["ar"]`.
- **بيانات ثنائية:** في الـ`*Data.ts` كل عنصر يحمل النسختين، ودالة `map*` بتختار: `en && enVal ? enVal : ar` (لاحظ `||` مش `??` — الفراغ يرجع للعربي).
- **RTL:** استخدم خصائص Tailwind المنطقية (`ms/me/ps/pe/start/end/text-start`) — تنقلب تلقائيًا. للأيقونات الاتجاهية أضف كلاس `dir-flip`. الـ`dir` على الحاوية = `rtl`/`ltr` حسب اللغة.
- **قائمة القوائم في اللوحة:** أي عرض لعنوان عنصر يجب أن يتبع لغة اللوحة (يعرض `_en` في الإنجليزي، ويرجع للعربي لو فارغ) — لا تثبّت `_ar` (خطأ وقعنا فيه).

---

## 9. نظام الـCMS (schema-driven)

**الفكرة:** لا تكتب محرّرًا لكل نوع. عرّف الموديل → **المحرّر يتولّد** من الـschema.

**المصادقة:** توكن في `localStorage` (`oboor_cms_token`)، يُرسَل `Authorization: Token <token>` — يعمل خلف أي بروكسي (لا كوكيز/CSRF).

**Endpoints (`/cms/…`):**
- `GET /cms/collections/{type}/schema/` → `{ fields, readonly, group_field }` (schema مولّد من الموديل).
- `GET/POST /cms/collections/{type}/` — قائمة/إنشاء. `GET/PATCH/DELETE /cms/collections/{type}/{pk}/`.
- `POST …/{pk}/reset/` (استعادة الافتراضي)، `POST …/reorder/` (ترتيب).
- `POST /cms/preview/` (مسودّة معاينة)، `POST /cms/upload/` (رفع صورة عامة).
- سجلّ النوع في `cms_crud.py`: `type → (Model, title_field, group_field)`.

**نوعان من المحتوى:**
1. **عناصر قوائم (Collections)** — لكل نوع موديل خاص (news/programs/branches/…)، يُحرَّر عبر **`CollectionEditor`**. الحقول تتولّد من الموديل؛ التجميع لأقسام عبر `FIELD_SECTIONS[type]`.
2. **أقسام الصفحات (Page sections)** — موديل واحد عام **`SectionItem`** بمفاتيح `page`/`block`/`key`/`order` + حقول عامة: `title_ar/en`, `text_ar/en`, `data_ar/en` (قائمة JSON), `tagline_ar/en`, `icon`, `value`, `color`, `image`, badges. الصفحات الثابتة (about/home/contact/assessment…) بتتحرّر عبر **`PageChrome`** (accordion لكل بلوك). **لتحويل أي عنصر ثابت لقابل للتحرير:** حوّله لصف `SectionItem` (page/block/key) واقرأه في الصفحة عبر `fetchSections`.

**مبدأ المرآة (Mirror Principle):** اللوحة تعكس **كل** الصفحة، **بترتيبها**، بلا زحام ولا حقول مخفية. حقول الإدارة (منشور/ترتيب) منفصلة وبلا معاينة.

**الجسر + السقوط الآمن:**
```ts
// src/lib/server/django.ts
export async function fetchContent<T>(path): Promise<T | null> {
  try { /* fetch مع timeout + cache */ } catch { return null; }  // ← الصفحة ترجع للثابت
}
// كاش: { next: { revalidate: 300, tags: ["cms-content"] } }  — يُبطَّل فورًا بعد الحفظ عبر
// server action: updateTag("cms-content")  (اقرأ-اكتب-فورًا / read-your-own-writes)
```

---

## 10. نمط «السكشن فيو» (Section-View) — الوصفة القابلة للتكرار

**الهدف:** كل قسم في محرّر العنصر = **عمودان**: يسار الحقول، يمين **معاينة حيّة مطابقة تمامًا لشكل القسم على الموقع**، تتحدّث مع كل حرف قبل الحفظ. حفظ موحّد للصفحة/العنصر كله.

**آلية التنفيذ (في عبور):**
1. **`FIELD_SECTIONS[type]`** (في `CollectionEditor.tsx`) — قائمة مجموعات؛ كل مجموعة: `{ title, title_en, bases: string[], note?, preview?: "/path/{slug}" }`. الحقول غير المُدرَجة → مجموعة «إعدادات» (بلا معاينة).
2. **`LIVE_PREVIEW_TYPES`** — Set بأنواع العناصر اللي تعرض المعاينة (بوّابة تدريجية: أضِف نوعًا بعد ما تبني معاينته).
3. **`GroupPreview({ type, bases, values, lang })`** (في `SectionPreview.tsx`) — موزّع يستدعي مكوّن معاينة **مخصّص لكل نوع** (`ProgramGroupPreview`, `BranchGroupPreview`…). كل مكوّن يفرّع حسب `bases` (أي مجموعة) ويرسم القسم بألوانه وتصميمه الحقيقي.
4. **صفحات PageChrome** تاخد المعاينة تلقائيًا عبر `BlockPreview` المشترك — لا شغل إضافي لكل صفحة.

**كيف تبني معاينة قسم مطابقة (الطريقة الصحيحة):**
> اقرأ **مكوّن الموقع الفعلي** للقسم (مثال `src/app/programs/[slug]/page.tsx`) → انسخ نفس البنية/الألوان في مكوّن معاينة مصغّر → وزّعه من `GroupPreview` حسب `(type, bases)` → deploy → **تحقّق بـPlaywright بلقطة**. لا معاينات «تقريبية» — مطابقة لكل قسم.

**قواعد أثبتناها:**
- **المعاينة تقرأ `values` مباشرة** (كل الحقول)، لا تتقيّد بـ`bases` مجموعتها فقط عند الحاجة (مثلًا الفوتر بيحتاج حقول من مجموعة تانية).
- **الأيقونات:** استخدم **نفس مصدر الموقع** — محتوى الأقسام يستخدم `iconByKey`/`distinctIcons` من `@/lib/areaIcon`، ومنتقي أيقونات الـCMS يستخدم `CMS_ICONS`. خلطهم = أيقونات مختلفة عن الموقع.
- **الصور في المعاينة:** `object-contain` بخلفية وارتفاع كافٍ (مش `object-cover`) — عشان المحرّر يشوف الصورة **كاملة** بعد رفعها.
- **النص الافتراضي:** لو حقل اختياري بيسقط لنص افتراضي على الموقع (زي CTA لكل عنصر)، وضّح ده في المعاينة بتنبيه «هذا نص افتراضي — املأ الحقل لتخصيصه» لمّا الحقل يكون فارغًا.
- **الـsticky للمعاينة:** `position: sticky` بيتكسر لو أي أب فيه `overflow: hidden/auto`. استخدم `overflow-x-clip` (مش `hidden`) وشيل `overflow-hidden` من الأغلفة.
- **عرض المحرّر:** المحرّرات ذات المعاينة توسّع لـ`max-w-7xl` مع نسبة حقول ~64% / معاينة ~36%.

---

## 11. كتالوج المكوّنات القابلة لإعادة الاستخدام

انسخ هذه الأنماط لأي مشروع جديد بنفس الستاك:

| المكوّن/الأداة | الوظيفة |
|---|---|
| `pick()` + `getLocale()` + `common.ts` | نظام i18n كامل (القسم 8). |
| `fetchContent()` + `*Data.ts` | جسر محتوى بسقوط آمن (القسم 7/9). |
| `CollectionEditor` + `FIELD_SECTIONS` + `PageChrome` | محرّر CMS مبني على schema (القسم 9). |
| `GroupPreview` / `*GroupPreview` | معاينات السكشن فيو الحيّة (القسم 10). |
| `CtaSection` | قسم تواصل موحّد (بادج + عنوان + وصف + أزرار) قابل للتخصيص لكل صفحة. |
| `highlight()` / `hl()` + `**bold**` | تمييز جزء من عنوان بلون البراند عبر نجمتين. |
| `seo.ts` / `pageMeta()` | metadata موحّد (عنوان 30-60، وصف 120-160، تحقّق GSC). |
| icon systems: `CMS_ICONS` (منتقي) + `areaIcon` (`iconByKey`/`distinctIcons`) | أيقونات المحتوى. |
| نمط الـmodal (SuccessStoryCard/Specialist) | كارت + نافذة تفاصيل inline (بدون صفحة منفصلة). |
| `cms_analytics.py` + `/cms/analytics` | لوحة تحليلات داخل الـCMS (GA4 Data API + Search Console) للأدمن. |

---

## 4/12. كتالوج الأخطاء (Problem → Fix)

### أخطاء البنية/النشر
| الخطأ | الإصلاح المعتمد |
|-------|------------------|
| تعارُض الاعتماديات في بناء Docker | أبقِ الـ lock file متزامنًا، ثبّت نسخة Node، واختبر بناء Docker **مبكّرًا**. احذر الاعتماديات الخاصة بالمنصّة (`npm install` لا `ci` عند اللزوم). |
| أسرار في المستودع | `.env.example` بقيم وهمية فقط؛ الأسرار الحقيقية تُوضع على السيرفر يدويًا/عبر scp للملف مباشرة (لا تُلصق في محادثات). |
| إعداد بيئة خاطئ (بادئة `/api`) | ملف بيئة واحد لكل خدمة، وطابِق صيغة المسارات بين الكود والبروكسي. |
| رابط قاعدة بيانات فيه محارف خاصة (`%`) | مرّر بيانات الاتصال منفصلة (host/port/user/pass) لا كـ URI. |
| اختلاف نسخة Postgres عند `pg_dump` | ثبّت نسخة أداة الـ dump = نسخة خادم الإنتاج. |
| SSL إجباري على Postgres محلي | اجعل SSL اختياريًا عبر متغيّر بيئة (`DATABASE_SSL=disable`). |
| محتوى مختلط على HTTPS | روابط أصول مطلقة بـ HTTPS على نفس الدومين، أو مسارات نسبية same-origin. |
| بورتات مكشوفة (Docker + ufw) | اربط على `127.0.0.1`؛ Docker يتجاوز ufw إن نشرت على `0.0.0.0`. |
| صفحة أدمن «مخفية» بلا حماية | بوّب كل واجهة أدمن خلف مصادقة — لا تعتمد على «الرابط سرّي». |
| بطء «الأداء» بعد النشر (77) على كاش بارد | فعّل **gzip** في nginx على CSS/JS (كانت غير مضغوطة على أول زيارة). |

### أخطاء الواجهة/الـCMS (من التجربة)
| الخطأ | الإصلاح |
|---|---|
| `sticky` لا يتبع السكرول | شيل `overflow: hidden/auto` من كل الآباء؛ استخدم `overflow-x-clip`. |
| الهيرو فاضي ~ثوانٍ عند التحميل | ليست الصورة بطيئة بل الإحساس — أضف `placeholder="blur"` (blur base64 صغير) + خلفية بلون الثيم للقسم. |
| صور المعاينة مقصوصة | `object-contain` + خلفية + ارتفاع أكبر (مش `object-cover`). |
| أسماء عربية في اللوحة الإنجليزية | اجعل عرض عنوان القائمة تابعًا للغة اللوحة (`_en` ثم يرجع `_ar`). |
| حقل «رقم» ثابت المفروض ديناميكي | احسبه من مصدره (مثال: «عدد الأسئلة» من طول قائمة الأسئلة) وأخفِ الحقل اليدوي. |
| أيقونات المعاينة ≠ الموقع | استخدم نفس مصدر الأيقونات (`areaIcon` للمحتوى، لا `CMS_ICONS`). |
| CTA لكل العناصر من مكان واحد | اجعل CTA **لكل عنصر** (حقول خاصة به) مع سقوط لنص افتراضي + تنبيه في المعاينة. |
| كاش المحتوى لا يتحدّث بعد تعديل DB مباشر | التعديل عبر الـCMS يُبطِل `cms-content` تلقائيًا؛ لو عدّلت DB يدويًا، أعد بناء الواجهة أو انتظر `revalidate`. |

### أخطاء البنية الخارجية (Google/بريد)
| الخطأ | الإصلاح |
|---|---|
| GA4/Search Console انكسرت فجأة | **لا تربط بنية العميل بإيميل شخصي/مؤقّت.** كل شيء تحت حساب العميل الرسمي (القسم 14). |
| SMTP على إيميل شخصي | انقله لحساب الشركة (App Password من حساب Workspace) أو خدمة إرسال (Brevo/Mailgun) — لا تعتمد على Gmail شخصي. |

---

## 5/13. مرجع النشر والتشغيل (Deployment & Ops Runbook)

**التشغيل الأول:**
```bash
# 1) الكود على السيرفر (git clone أو نقل الأرشيف)
# 2) ملفات البيئة في أماكنها:
#    ./.env            → متغيّرات docker compose (كلمات مرور Postgres/MinIO، NEXT_PUBLIC)
#    ./.env.local      → خدمة الواجهة (frontend)
#    ./backend/.env    → خدمة الباك إند (backend) — الأسرار
#    ./backend/secrets/ → ملفات أسرار (مثل ga-service-account.json) mounted :ro
# 3) التشغيل:
docker compose up -d --build
```

**دورة التحديث (اللي اتبعناها طول الجلسة):**
```bash
# 1) commit + push للريموتين (بدون trailer في رسالة الـcommit — عرف مستودع العميل)
git push origin <branch>  &&  git push company <branch>
# 2) انقل الملفات المتغيّرة للسيرفر (لا تعيد رفع كل حاجة)
scp -i $HOME/.ssh/team_key <files> root@<ip>:/opt/oobor/<path>     # على Git Bash: MSYS_NO_PATHCONV=1
# 3) أعد بناء الخدمة المتأثّرة فقط
ssh … "cd /opt/oobor && docker compose up -d --build frontend"    # أو backend
# 4) هجرات الباك (تُطبَّق عند إقلاع الحاوية؛ أو يدويًا):
#    docker compose exec -T backend python manage.py migrate content
# 5) تحقّق بـPlaywright على الموقع المباشر (لقطة/فحص).
```

**ملاحظات عملية:**
- بعد rebuild للـfrontend، الباك بيعيد الإقلاع كتبعية → **502 مؤقّت ~8ث** على أول طلب (سخّن ثم أعد التحميل).
- كاش تحسين صور Next يُمسح مع كل rebuild → أول زيارة بعد النشر تعيد ترميز الصور (استخدم blur placeholder للـLCP).
- **قاعدة ملفات البيئة:** خدمة واحدة = ملف بيئة واحد:
```yaml
backend:   env_file: ./backend/.env
frontend:  env_file: .env.local
# postgres / minio: تقرأ من ./.env عبر ${VAR}
```
- **البنية:** `Cloudflare (SSL) → nginx:80 → [frontend:3000 | backend:8000] → Postgres + MinIO` — كلها على `127.0.0.1`.

---

## 14. التحليلات والبنية الخارجية (Google/Analytics)

**القاعدة الحاكمة:** **لا تربط أي بنية خارجية (GA4/Search Console/Cloud/SMTP) بإيميل شخصي أو مؤقّت أبدًا** — كلها تحت حساب العميل الرسمي (Workspace). سبب: إن اتقفل الحساب المالك، كل حاجة تنكسر.

**تتبّع GA4:** `GA_ID` (Measurement ID) في `layout.tsx`؛ يُحمَّل عبر gtag **مؤجّل** (لا يحجب التحميل). قابل للتبديل بـ`NEXT_PUBLIC_GA_ID`.

**لوحة التحليلات داخل الـCMS** (`cms_analytics.py`, `/cms/analytics`) — للأدمن فقط، تقرأ عبر **service account**:
- **GA4 Data API:** `GA4_PROPERTY_ID` + ملف مفتاح service account (`/app/secrets/ga-service-account.json`، mounted). الـSA يُضاف كـViewer على الـProperty.
- **Search Console API:** نفس الـSA يُضاف كـuser على موقع GSC؛ `GSC_SITE_URL`.

**نقل لحساب العميل (وصفة مجرّبة):**
1. property/موقع جديد تحت حساب الشركة (أو نقل ملكية).
2. مشروع Google Cloud جديد تحت حساب الشركة → فعّل «Google Analytics Data API» + «Search Console API».
3. Service account جديد → مفتاح JSON → **ارفعه للسيرفر بـscp مباشرة** (لا تلصق محتواه في محادثة).
4. أضِف الـSA كـViewer على الـProperty، وكـuser على موقع GSC.
5. حدّث `GA_ID` + `GA4_PROPERTY_ID` + أعد نشر الباك، وتحقّق (`_ga_client().run_report(...)` يرجع بيانات).
6. تحقّق ملكية GSC لحساب الشركة بطريقة **HTML file** (ملف في `public/google*.html` — لا تعتمد على DNS دومين لا تملكه).

---

## 6/15. تشغيل مشروع جديد بنفس الستاك — Bootstrap Checklist

> «انسخ البنية لا الكود.» عبور هو القالب المرجعي.

- [ ] **1. القالب:** ابدأ من نفس بنية المجلدات (`src/app|components|lib|i18n`, `backend/content` بنمط `m_/api_/a_/cms_`).
- [ ] **2. الستاك:** ثبّت نفس النسخ (القسم 1). اقرأ `node_modules/next/dist/docs/` (Next حديث ≠ ذاكرتك).
- [ ] **3. i18n أولًا:** انسخ `pick`/`getLocale`/`common.ts`؛ خصائص منطقية؛ لا نص hard-coded.
- [ ] **4. رموز التصميم:** ألوان/خطوط/مسافات من Figma → tailwind config (مصدر واحد).
- [ ] **5. نموذج المحتوى:** لكل نوع: موديل ثنائي اللغة (`*_ar/_en`) + `*Data.ts` ثابت بنفس الشكل + `fetchContent` بسقوط آمن.
- [ ] **6. الـCMS:** محرّر schema-driven + توكن auth + مبدأ المرآة + **سكشن فيو لكل نوع** (القسم 10).
- [ ] **7. الحاويات من الأسبوع الأول:** Dockerfile/compose/`.env.example` + بورتات على `127.0.0.1` + nginx + Cloudflare.
- [ ] **8. البنية الخارجية على حساب العميل** من البداية (القسم 14) — لا إيميلات شخصية.
- [ ] **9. الدليل:** admin guide بالصور خلف المصادقة.
- [ ] **10. اختبر على المباشر** (Playwright) قبل التسليم، وحدّث هذا الملف مع كل تغيير جوهري.

---

## 16. اصطلاحات المستودع

- `.gitignore`: تجاهل `.env*` مع استثناء `!*.env.example`.
- تسمية الفروع والـ commits واضحة؛ **لا أسرار في الرسائل أو التاريخ**؛ لا trailers غير مرغوبة (عرف مستودع العميل).
- كل خدمة لها `Dockerfile` و`.dockerignore` خاص بها.
- فرع ميزة كبير → merge لـ`main` بعد الاختبار (fast-forward نظيف).
- **التوثيق (هذا الملف + دليل المستخدم) يُحدَّث مع كل تغيير جوهري** — هو جزء من التسليم، لا رفاهية.
